{% extends "@application/layout/layout.twig" %}
{% block content %}
    <div class="container">
        <div class="row cms-page">
            <pre>{{ dump(checkoutService) }}</pre>

            {% for paymentMethod in checkoutService.paymentMethods %}
                <pre>{{ dump(paymentMethod) }}</pre>
            {% endfor%}
        </div>
    </div>
{% endblock %}
