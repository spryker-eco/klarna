{% extends "@application/layout/layout.twig" %}
{% block content %}
    <div class="container">
        <div class="row cms-page">
            <pre>{{ dump(installments) }}</pre>
            {% for pClass in installments.payments %}
                <pre>{{ dump(pClass) }}</pre>
            {% endfor%}
        </div>
    </div>
{% endblock %}
